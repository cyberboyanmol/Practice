#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    int t, N, Q;
    while (t--)
    {
        cin >> N >> Q;
        ll arr[N];
        ll type, L, R, val;
        for (int i = 0; i < N; i++)
        {
            cin >> arr[i];
        }
        cin >> type >> L >> R;
        for (int i = 0; i < N; i++)
        {
            if (type == 1)
            {
                index =
            }
        }
    }
    return 0;
}