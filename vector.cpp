#include <bits/stdc++.h>
using namespace std;

int main()
{
    // vector<int> arr;
    // for (int i = 0; i < 9; i++)
    // {
    //     int x;
    //     cin >> x;
    //     arr.push_back(x);
    // }
    // // cout << arr.capacity();
    // // cout << arr.size();
    // // arr.clear();
    // // cout << arr.size();
    // // cout << arr.capacity();
    // // vector<int> vec2(arr.begin(), arr.end());
    // //cout << arr.at(0);
    // vector<int> vec2(arr.begin(), arr.begin() + 3);
    // // vec2.push_back(7);
    // cout << vec2.size();
    // cout << vec2.capacity();
    // for (int i = 0; i <= vec2.size(); i++)
    // {
    //     cout << vec2.at(i) << "";
    // }

    /*  in emplace_back() take lessess time then push_back()*/

    return 0;
}